
  {{> studentDashboardHeader }}
  
<div 
  hidden 
  aria-hidden='true' 
  id='js-timer-raw-data'
  data-start-time='{{Tasks.start_time}}'
  data-duration='{{Tasks.timer_duration}}'
  data-uuid='{{Tasks.uuid}}'
>
</div>


<div class="student-dashboard-dash" id="container">
  <div class="row">
    {{>studentTimer}}
    <section>
      <div class="student-dashboard-right col-m-6">
        <div class="task-title">
          Task
        </div>
        <h3 class="task-value col-m-6">Earn {{Tasks.coin_value}} Coin(s)!</h3>
        {{#if Tasks.name}}
        <div class="task-desc col-m-6" id="task-name">
          {{Tasks.name}}
        </div>
        {{/if}}
        {{#if Tasks.description}}
        <div class="task-desc-body col-m-6" id="task-description">
          {{Tasks.description}}
        </div>
        {{/if}}
        {{#if Tasks.name}}
        <button id="js-timer-done" class="task-done-btn text-align-center col-12">
          DONE!
        </button>
        {{/if}}
      </div>
    </section>
  </div>
</div>